@inherits LayoutComponentBase

<PageTitle>Transformer-Forecasting-Web</PageTitle>

<MudThemeProvider />
<MudDialogProvider />
<MudSnackbarProvider />

<MudLayout>
    <MudAppBar>
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
        My Application
    </MudAppBar>
    <MudDrawer @bind-Open="@_drawerOpen">
        <MudNavMenu>
    <MudNavLink Href="/" Match="NavLinkMatch.All">Make prediction</MudNavLink>
    <MudNavLink Href="/fetchdata"  Match="NavLinkMatch.Prefix">fetchdata</MudNavLink>
    <MudNavLink Href="/counter" Match="NavLinkMatch.Prefix">Counter</MudNavLink>
    <MudNavLink Href="/settings"  Match="NavLinkMatch.Prefix">Settings</MudNavLink>
    <MudNavLink Href="/loadcalculator"  Match="NavLinkMatch.Prefix">Load Calculator</MudNavLink>
</MudNavMenu>
    </MudDrawer>
    <MudMainContent>
        @Body
    </MudMainContent>
</MudLayout>



@code {
    bool _drawerOpen = true;

    void DrawerToggle()
    {
        _drawerOpen = !_drawerOpen;
    }
}